import { Component } from '@angular/core';
import { CommonModule } from '@angular/common';

@Component({
  selector: 'lib-<% name %>',
  imports: [CommonModule],
  template: `<p><%= String(name).charAt(0).toUpperCase() + String(name).slice(1) %> works!</p>`,
  styles: ``,
})
export class <%= String(name).charAt(0).toUpperCase() + String(name).slice(1) %>Component {}
